apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.retrieverMilvus.name }}
spec:
  type: {{ .Values.retrieverMilvus.service.type }}
  ports:
    - port: {{ .Values.retrieverMilvus.service.port }}
      targetPort: {{ .Values.retrieverMilvus.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.retrieverMilvus.name }}

